# Backend Environment Variables
HOST=0.0.0.0
PORT=1337

# Generate these with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
APP_KEYS=generateRandomKey1,generateRandomKey2,generateRandomKey3,generateRandomKey4
API_TOKEN_SALT=generateRandomSalt
ADMIN_JWT_SECRET=generateRandomSecret
TRANSFER_TOKEN_SALT=generateRandomSalt
JWT_SECRET=generateRandomSecret

# Database
DATABASE_CLIENT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=kungpao
DATABASE_USERNAME=kungpao
DATABASE_PASSWORD=kungpao_dev_password
DATABASE_SSL=false

# Frontend Environment Variables
NEXT_PUBLIC_STRAPI_API_URL=http://localhost:1337
STRAPI_API_TOKEN=your-strapi-api-token-here

# Node Environment
NODE_ENV=development
